<link rel="stylesheet" href="/css/home/form.css">

<section>
    <h2>Novo Produto</h2>
    <form action="/revendedora/edit-produto/{{produto.id}}" method="POST">
        <input type="text" name="nome" placeholder="Nome do Produto" required value="{{produto.nome}}">
        <input type="number" min="0" step="0.01" name="preco" placeholder="Preço" required value="{{produto.preco}}">
        <input type="text" name="link" id="link" placeholder="Link (opcional)" value="{{produto.link}}">
        <button type="submit">Salvar</button>
    </form>
</section>