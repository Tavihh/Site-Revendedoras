<link rel="stylesheet" href="/css/admin/painel.css">

<a href="/loja/{{loja.id}}/{{loja.nomeLoja}}" class="link-loja">http://revendedora.xyz/loja/{{loja.id}}/{{loja.nomeLoja}}</a>
<hr>
<h2>Painel de Produtos</h2>

<div>
    <table>
        <thead>
            <tr>
                <td class="imagem">Foto</td>
                <td>Nome</td>
                <td>Preço</td>
            </tr>
        </thead>
        <tbody>
            {{#each produto}}
            <tr onclick="location.href='/admin/produto/editar/{{id}}'">
                <td class="imagem"><img src="/produtos/{{path_foto}}" alt=""></td>
                <td>{{nome}}</td>
                <td>{{preco}}</td>
            </tr>
            {{/each}}
        </tbody>
        <tfoot>
            <tr>
                <td colspan="3">
                    Total Produtos: {{produto.total}}
                </td>
            </tr>
        </tfoot>
    </table>
</div>